import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact',
  description: 'Get in touch with the Electronic Store team.',
};

export default function ContactPage() {
  return <div>{/* Contact page content */}</div>;
}
